commit 044d25e22e2bebabcd3da21d276a831cc124ef17
Author: William Gearty <willgearty@gmail.com>
Date:   Thu Apr 2 11:10:39 2026 -0400

    Update site.webmanifest

commit b4c16f67e9142542d84c40afa223021f5c86822b
Merge: 0768321bc b83ca76c8
Author: Supriyo Chowdhury <supriyochowdhury760@gmail.com>
Date:   Thu Apr 2 13:53:00 2026 +0530

    Merge origin/main, resolve conflicts, and strip BOM zero-width characters

commit b83ca76c878b9ef7b1480d299d05f598b71ebd40
Author: Paras Joshi <pjisgoodenough2006@gmail.com>
Date:   Thu Apr 2 01:08:07 2026 +0530

    Fix seed_dummy_data to seed ProgramModuleObj instances for each program (#5564)
    
    * Add dynamic admin toolbar links feature
    
    * remove the unnecessary code
    
    * Refactor dynamic toolbar links to use theme settings instead of database model
    
    * Rework dynamic toolbar links to use theme settings instead of DB model
    
    * Fix toolbar links rendering using json_script for safe JSON serialization
    
    * Fix theme editor crash when images/theme contains a subdirectory  (#4204)
    
    * Fix theme editor crash when images/theme contains a subdirectory (#1764)
    
    get_file_summaries() was checking os.path.isdir(filename) using the bare
    filename (relative to CWD) instead of the full path. This caused it to
    always evaluate False for subdirectories, then attempt to open() the
    directory as a file, raising IsADirectoryError during recompile.
    
    Fix: use os.path.isdir(full_filename) so subdirectories are correctly
    recursed into rather than opened as files.
    
    * Fix path traversal vulnerability in theme file path handling (CodeQL)
    
    base_dir() now resolves the full path with os.path.realpath() and
    verifies it stays within THEME_PATH before use. A theme_name like
    '../../etc/passwd' from request.POST would previously have been passed
    directly into os.path.join() and used in file operations.
    
    Similarly, logo_select / header_select / favicon_select from POST data
    were directly concatenated into file paths without validation. Added
    _safe_backup_path() helper in views.py that resolves and checks the
    path stays within the backups directory before copyfile().
    
    * Address PR review comments: fix file handle leak, remove redundant import, add tests
    
    - Use context manager for open() in get_file_summaries() to prevent
      resource leak during bulk file processing
    - Remove redundant `import os.path` in views.py (already available via `import os`)
    - Add BaseDirPathTraversalTest: 4 tests covering valid theme names and
      path traversal attempts in ThemeController.base_dir()
    - Add SafeBackupPathTest: 4 tests covering valid filenames and path
      traversal attempts in _safe_backup_path()
    
    * Fix missing newlines at end of files
    
    * fix indentation error
    
    * fix migration check error
    
    * minor fix
    
    * fix copilot review issues
    
    * lint test fail fix
    
    * minor fix
    
    * Revert changes from #4204
    
    * Restore comments; revert indentations
    
    * Moved toolbar_links section to bottom
    
    * minor fix
    
    * minor fix
    
    * final fix
    
    * Fix seed_dummy_data to seed ProgramModuleObj instances for each program
    
    * Fix seed_dummy_data to seed ProgramModuleObj instances for each program
    
    * Address Copilot review: reuse queryset, backfill unconditionally, add test assertion
    
    * Expand all_module_handlers to cover student, teacher, and admin modules
    
    * Update esp/esp/program/management/commands/seed_dummy_data.py
    
    Co-authored-by: William Gearty <willgearty@gmail.com>
    
    * Address review: fix module categorizations and add TeacherBigBoardModule
    
    ---------
    
    Co-authored-by: Paras Joshi <paras521851@gmail.com>
    Co-authored-by: Oval <216409111+Oval17@users.noreply.github.com>
    Co-authored-by: William Gearty <willgearty@gmail.com>
