# Django
Django==3.1.14

# Django-specific packages
git+https://github.com/learning-unlimited/django-argcache.git@django2 # Handles caching
django-debug-toolbar==3.2.4 # Provides the debug toolbar (compatible with Django 3.0)
django-extensions==3.1.5 # Provides shell_plus, runserver_plus, and other utilities (compatible with Django 3.0)
django-filebrowser-no-grappelli==4.0.2 # Provides admin panel interface for files
django-form-utils==1.0.3 # Provides BetterForm (used in customforms) and BetterModelForm (used in program creation)
django-formtools==2.3 # Used once in customforms (compatible with Django 3.0)
django-localflavor==3.1 # Provides address fields (compatible with Django 3.0)
django-phonenumber-field[phonenumbers]==6.3.0 # Provides phone number fields (compatible with Django 3.0)
django-recaptcha==4.0.0 # Provides support for Google reCAPTCHA
django-reversion==5.0.2 # Handles versioning, currently for QSD and TemplateOverrides (compatible with Django 3.0)
django-sendgrid-v5==1.2.4 # Provides support for using SendGrid as the EmailBackend
django-vanilla-views==3.0.0 # Provides simpler generic views, currently used only for grade change requests
git+https://github.com/learning-unlimited/django-admin-tools.git@esp # Extends Django Admin


# Non-Django-specific packages
bleach==6.0.0 # For sanitizing QSD content
dill # For migrating Python 2 pickles in the database
docutils==0.12 # Needed for django.contrib.admindocs
flake8==7.0.0 # Required for linter
ipython==8.12.3 # Used for shell_plus (8.12 is last supporting Python 3.8)
Markdown==3.6 # Used in QSD
numpy==1.26.4 # Used mainly in the lottery and class change controller
pillow==10.3.0 # Required for ImageField, which we use in teacher bios
psycopg2==2.9.9 # Talks to postgres
pycurl==7.45.3 # Used only in a formstack script which I think is outdated
dnspython==2.6.1 # Used for validating email addresses; replaces py3dns
Pygments==2.18.0 # django-extensions dependency
pylibmc==1.6.3 # Talks to memcached
pytz==2024.1 # Required for timezone support
# pywatchman # Makes dev servers auto-reload
raven # Required for Sentry error reporting
setuptools>=75.0.0 # For building packages from source; stripe and a few others don't work with v58+
selenium==4.21.0 # Runs selenium tests which probably don't work anymore
shortuuid==1.0.13 # django-extensions dependency
stripe==9.6.0 # Required for credit card processing
twilio==9.1.0 # Required for text messaging support
wheel==0.43.0  # For building modern packages
werkzeug==3.0.3 # Required for runserver_plus (alternative to runserver provided by django-extensions)
xlwt==1.3.0 # Does form and survey exporting

