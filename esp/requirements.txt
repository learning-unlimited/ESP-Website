# Django
Django==4.2.30

# Django-specific packages
git+https://github.com/learning-unlimited/django-argcache.git@v0.4 # Handles caching
django-debug-toolbar==4.4.0 # Provides the debug toolbar
django-extensions==3.2.3 # Provides shell_plus, runserver_plus, and other utilities
django-filebrowser-no-grappelli==4.0.0 # Provides admin panel interface for files
git+https://github.com/learning-unlimited/django-form-utils.git@v1.1.0 # Provides BetterForm (used in customforms) and BetterModelForm (used in program creation)
django-formtools==2.4 # Used once in customforms
django-localflavor==3.1 # Provides address fields
django-phonenumber-field[phonenumbers]==6.3.0 # Provides phone number fields
django-recaptcha==3.0.0 # Provides support for Google reCAPTCHA
django-reversion==5.0.2 # Handles versioning, currently for QSD and TemplateOverrides
django-sendgrid-v5==1.2.4 # Provides support for using SendGrid as the EmailBackend
django-vanilla-views==3.0.0 # Provides simpler generic views, currently used only for grade change requests
git+https://github.com/learning-unlimited/django-admin-tools.git@v0.9.4+esp.1 # Extends Django Admin


# Non-Django-specific packages
bleach==6.0.0 # For sanitizing QSD content
dill # For migrating Python 2 pickles in the database
docutils==0.19 # Needed for django.contrib.admindocs
Faker==18.13.0 # For generating dummy/seed data
flake8==7.1.1 # Required for linter
pytest==7.4.4 # Test runner
pytest-cov==4.1.0 # Coverage measurement compatible with pytest-xdist parallel runs
pytest-django==4.5.2 # Django integration for pytest
pytest-xdist==3.3.1 # Parallel test execution via -n auto
ipython==8.12.3 # Used for shell_plus
Markdown==3.4.4 # Used in QSD
numpy==1.26.4 # Used mainly in the lottery and class change controller
pillow==12.2.0 # Required for ImageField, which we use in teacher bios
psycopg2==2.9.9 # Talks to postgres
pycurl==7.45.4 # Used only in a formstack script which I think is outdated
py3dns==4.0.2 # Used for validating email addresses; imports as "DNS" (case matters), not "py3dns"
Pygments==2.10.0 # django-extensions dependency
pymemcache==4.0.0 # Talks to memcached
pytz==2023.3 # Required for timezone support
# pywatchman # Makes dev servers auto-reload
raven # Required for Sentry error reporting
setuptools==70.3.0 # For building packages from source
selenium>=4.9,<5 # Runs selenium tests
shortuuid==0.4.2 # django-extensions dependency
stripe==7.5.0 # Required for credit card processing
twilio==6.63.2 # Required for text messaging support
wheel==0.38.4  # For building modern packages
werkzeug==2.0.3 # Required for runserver_plus (alternative to runserver provided by django-extensions)
openpyxl==3.1.2 # Does form and survey exporting
