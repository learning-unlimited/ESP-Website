import esp.mailman
from esp.users.models import ESPUser, User

def get_teachers_by_section(section):
    #get what the website thinks are the teachers

def teacher_list_check(section):
    """Compare the people on the teacher list for a given class section to what we expect

The argument should be, for example, x5950s1"""
    #find what mailman thinks:
