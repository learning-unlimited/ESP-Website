""" Local settings for Docker-based development environment. """

import os

SITE_INFO = (1, 'devsite.learningu.org', 'LU Dev Site')
# CACHE_PREFIX removed - replaced by KEY_PREFIX inside CACHES
# See: https://github.com/learning-unlimited/ESP-Website/issues/1306
PROJECT_ROOT = os.path.join(os.path.dirname(os.path.realpath(__file__)), '..')
USE_MAILMAN = False
DEBUG = True
SHOW_TEMPLATE_ERRORS = DEBUG
CACHE_DEBUG = False

# Database — connects to the 'db' Docker Compose service
DATABASES = {'default':
                {'NAME': 'devsite_django',
                 'HOST': 'db',
                 'PORT': '5432',
                 'ENGINE': 'django.db.backends.postgresql_psycopg2',
                 'USER': 'esp',
                 'PASSWORD': 'password',
                }
            }

CACHES = {
    'default': {
        'BACKEND': 'django.core.cache.backends.memcached.PyLibMCCache',
        'LOCATION': 'memcached:11211',
        'TIMEOUT': 0,
        'KEY_PREFIX': 'ludev',  # replaces CACHE_PREFIX = "ludev"
    }
}

MIDDLEWARE_LOCAL = []

EMAIL_HOST_SENDER = 'devsite.learningu.org'
VARNISH_HOST = None
DEBUG_TOOLBAR = True
EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'
CLOSURE_COMPILER_PATH = '/usr/lib/closure/bin'

SECRET_KEY = 'docker-dev-secret-key-not-for-production'

ALLOWED_HOSTS = ['*']