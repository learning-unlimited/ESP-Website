Ext.define('LU.model.Program', {
    extend: 'Ext.data.Model',

    config: {
        idProperty: 'id',

        fields: [
            'title',
            'baseUrl',
            'id'
        ],
    }
});
